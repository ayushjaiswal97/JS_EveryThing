// 5. Ternary Operator
// condition ? trueValue : falseValue

// Write a program to check if a number is even or odd using a ternary operator.
let num = 10;
console.log(num%2===0 ? "even" : "odd");

// Use ternary to assign "pass" or "fail" based on marks.
let marks = 40;
let results = marks >= 33 ? "pass" : "fail";
console.log(results);